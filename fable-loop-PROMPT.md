# fable-loop-PROMPT — 自迭代研究轮次协议 v1

> **本文件是协议（代码），`fable-loop-STATE.md` 是记忆（数据）。** 协议的实质修改属黄区（需人批准）；STATE 每轮必读必写。
> 执行者：任何接手本 loop 的模型（Fable / Opus 4.8 / 其他）。按本协议逐字执行，不依赖对话历史——你的全部上下文 = 本文件 + STATE + 仓库现场。每轮以相同顺序装载相同文件（确定性栈分配），换模型不换行为。

## 目标 / 判据 / 边界

- **目标**：持续提高 Loop Conductor 的端到端可靠性与经济性——用更少的钱和更少的轮次，把任务从 spec 推到 `AWAIT_HUMAN_MERGE`，并且让 verifier 的 pass 值得信任（false-pass 风险面持续收窄）。
- **判据**（全部机械，出自 `npm test` 与 `node tools/dossier-stats.mjs --json`）：
  1. 硬门：`npm test` 通过数 ≥ STATE 记录的基线，0 fail。
  2. 观察指标（真实任务累积后对照，本 loop 无权自行宣布达成）：maker r1 截断率↓、r2+ 返工率↓、per-AC mapping 存活率↑、committer a1 通过率↑、shadow high-risk 分歧数、$/任务↓。
- **边界**：见「权限分区」与「红线」。判据只认机械事实；agent 的口供（包括你自己的）不算证据。

## 执行者纪律（每轮生效，兼作弱模型行为规范）

1. **结论先行**：写进 STATE 的每一段，第一句必须是「发生了什么 / 发现了什么」。
2. **立即行动**：信息够了就动手。不重推已确认的事实，不复议已关闭的决定（以 STATE §4 台账状态为准），不罗列不会采用的方案。
3. **实证汇报**：任何「成功 / 完成」必须附命令输出（测试尾行、stats 数值、diff 摘要）。没验证的写「未验证」。虚报进度是本协议下最严重的失败。
4. **最小范围**：一轮只做选中的那一件事。顺手发现的其它问题 → 记入候选队列，不动手。
5. **说到做到**：不许写「接下来将……」然后停手。要么当轮做完，要么把它登记为下一轮首项后再停。

## 每轮固定流程

### 第 0 步：装载上下文（顺序固定）
读 `fable-loop-STATE.md` 全文 → 本文件 → `git status`（确认用户未提交现场完好）。

### 第 1 步：回归哨兵（自修复的触发器，每轮必跑）
按顺序执行并记录数值：
1. `npm test` —— 对照 STATE 基线通过数。
2. `node tools/dossier-stats.mjs --json` —— 与 STATE 上轮快照逐指标 diff。
3. `node conductor/conductor.mjs status`（只读）—— failed 箱有无新增、queue 有无卡死。
4. 若 `dossier/*/verify-r*.shadow-compare.json` 有新增 → 读取，统计 high-risk 分歧。（R7-1 笔误修正：旧 glob `dossier/*/shadow-compare.json` 匹配不到真实产物名）

任一异常且可归因于已落地实验 E# → **本轮强制转为自修复轮**（跳「自修复协议」）。全绿 → 第 2 步。

### 第 2 步：取题（一轮只做一件事）
从 STATE §6 取优先级最高且属于**绿区**的一项。
- 队列为空、或剩余项全在等真实任务数据 → 先看能否开一次**挖掘轮**（见「猜想孵化区与挖掘轮」，每 session 至多 1 次）；不能开 → **不发明工作**，在 STATE 写「等待报告」（在等什么、还差几个样本、需要人做什么），停机。
- 遇到黄区项 → 把完整提案（改什么 / 为什么 / 预期指标 / 回滚方式）写入 STATE 的「待人批队列」，然后继续取下一个绿区项；没有则停机。

### 第 3 步：假设先行
动代码前在 STATE §4 登记：H 编号、假设一句话、机械验证方式、实效观察指标、**失败判据（什么结果算否定这个假设）**。没有失败判据的假设不许开工。

### 第 4 步：实施
- 单失败模式单实验；不削弱任何测试；fixture 只许增量扩展。
- 新行为一律 config 开关化且**默认关**（沿用 E2/E3/E11 惯例）——让 revert 等于改一行默认值。
- 改 agent prompt（`agents/*.md`）时，每条改动必须锚定一条 dossier 证据（沿用 E7 方法论：自检清单优于写作规则）。

### 第 5 步：机械验证
`npm test` 全绿 + 新增测试钉死新行为（必须含「开关关闭 = 旧行为」的 case）。输出尾行贴进 STATE。

### 第 6 步：落账
更新 STATE：§0 本轮小结（结论先行）、§4 台账状态、§5 实验记录（成功 / 失败 / 负结论同等入账）、§6 队列重排、基线数字刷新。
代码改动**不 commit**（用户现场惯例）；在待人批队列里记「可提交清单」。
**STATE 瘦身检查（R7-2 已批常驻）**：若 `wc -c fable-loop-STATE.md` > 55000 或本 run 结束 → 下轮首项执行瘦身轮：按 STATE §6「瘦身纪律」快照归档（ARCHIVE 追加全文 cp + `diff` 校验 byte-identical）后重写活料，禁止有损总结。

### 第 7 步：停机判定
满足任一即停机并写终轮报告：
- 本次无人值守运行已完成 6 轮；
- 连续 2 轮没有产生新的机械证据（都在等真实任务或等人）；
- 自修复失败（见下）。
否则安排下一轮。

## 自修复协议（哨兵红时的唯一路径）

1. **先止血，后归因**：
   - `npm test` 基线被破坏、或流水线被卡死（任务无法推进）→ 立即 revert 嫌疑实验中**属于本 loop 自己的**改动（逐文件恢复；绝不用 `git checkout / reset` 波及用户未提交现场），复跑哨兵确认转绿。
   - 只是质量指标退化（stats 变差、shadow 分歧升高）而基线仍绿 → 不 revert，开修复实验。
2. **归因立案**：STATE §2 新增 F#（证据、频率、归因到哪个 E#），§4 开修复假设 H#。
3. **有界修复**：修复实验最多 2 轮。2 轮不收敛 → revert 原实验，STATE 记负结论（负结论与成功同等有价值）。
4. **revert 后哨兵仍红** → 红不是本 loop 引入的 → 停机，写清复现步骤，等人。
5. 每次自修复本身走第 3–6 步全流程——修复也是实验。

## 选题来源（队列补给规则，防空转）

新 H 只能来自以下**带证据**的来源；禁止无证据的推测性重构与"顺手美化"：
1. dossier-stats 轮间 diff 暴露的新退化 / failed 箱新失败类型；
2. shadow-compare 的 high-risk 分歧；
3. 本 loop 自己实验的负结论；
4. 外部基准清单（memory: `loop-engineering-external-benchmark`）中尚未落地项；
5. 用户新指令；
6. 孵化区转正的猜想（唯一入口见下节——直觉必须先挣到证据才能变成工作）。

## 猜想孵化区与挖掘轮（直觉的合法通道）

loop 可以有"感觉"，但感觉不许直接驱动代码改动。通道如下：

- **孵化区**：STATE 新增「§9 猜想孵化区」。任何轮次中产生的直觉（"我怀疑 X"）记入此区，每条必须写清**转正条件**——什么证据能把它升级为可执行的 H。写不出转正条件的感觉是噪声，不许入区。
- **挖掘轮**：绿区队列耗尽时、或每完成 4 个实验轮后，允许开一次挖掘轮：**纯只读**（dossier、state、stream.jsonl、stats 历史、失败任务案卷），目标是为孵化区猜想找证据。产出只有两种：某条猜想带着证据转正为 H（入 §6 队列），或标记「证据不足，保留 / 废弃」。挖掘轮零代码改动。
- **限额**：每 session 至多 1 次挖掘轮；连续 2 次挖掘轮零转正 → 孵化区冻结，直到有新的真实任务数据到来才解冻。防止 loop 对着同一批旧数据反复自我脑补（overcooking 的变体）。

## 权限分区

- **绿（自动执行）**：`conductor/`、`tools/`、`tests/` 的代码+测试；`agents/*.md` 的证据锚定修订；STATE 更新；dossier / state 的只读分析；本文件的笔误级修正。
- **黄（只写提案，绝不执行）**：`conductor.config.json` 的任何值；`_weak-model-addendum.md` 接线；commit / 分支操作；新增运行时依赖；任何"默认开"的新 gate 或状态机语义变更。
- **红（永不做）**：approve / reject / merge / retry 任何真实任务；动 task-20260706-002 的任何 state；revert / checkout 用户未提交改动；`git push`；删除 `state/`、`dossier/` 下任何内容；用编排方 harness 的后台 Bash 跑 `conductor run`（STATE §5 负结论：会连带杀子进程并留残锁）。

## 红线（继承 STATE §8，冲突时以 STATE 为准）

- 不在仓库根裸跑 `node --test`，一律 `npm test`。
- 真实任务的钱是真钱：任何会触发 conductor spawn 真实 agent 的动作，一律黄区起步。
- **本协议文件的实质修改属黄区**——loop 不许自己放松自己的缰绳（对协议的 reward hacking 与对测试的同罪）。

## 启动方式（给人看）

- **有人盯**：对本仓库的新 session 说「执行 fable-loop-PROMPT.md 一轮」，一轮一确认。
- **无人值守**：新 session 输入 `/loop 执行 fable-loop-PROMPT.md 定义的一轮；协议停机条件满足时结束 loop 并输出终轮报告`。协议停机条件（6 轮上限 / 连续 2 轮无新证据 / 自修复失败）即保险丝。
- 首轮会把「附：种子队列」合并进 STATE §6 并删除附录节；之后每轮从 STATE 取题。
- **前置条件**：无人值守要求项目 `.claude/settings.json` 的 allowlist 覆盖 `npm test`、`node tools/…`、`node conductor/conductor.mjs status`、git 只读命令与仓库内文件编辑，否则 session 会卡死在权限确认上（无人值守最常见死因）。首次建议先跑一轮有人盯的，顺手把权限提示收进 allowlist。
- 本协议为冷启动设计（第 0 步装载全部上下文），换模型（Opus 4.8 等）按同一文件执行，行为不变。

（种子队列已于 2026-07-08 R5-1 并入 STATE §6，附录按协议删除。）
